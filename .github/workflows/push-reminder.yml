name: Push Reminder Scheduler

on:
  schedule:
    # 매 2시간마다 실행 (UTC 기준)
    - cron: '0 */1 * * *'

jobs:
  send_push:
    runs-on: ubuntu-latest
    steps:
      - name: Call push reminder API
        run: |
          curl -X POST https://your-service-name.onrender.com/api/push/reminder
